{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
  //app seting json somthing new is added for jenkins only to automate deployment from github to jenkins application and this is happening now and nothing else
}
